<p id="notice"><%= notice %></p>

<p>
  <b>Value:</b>
  <%= @phone_number.value %>
</p>

<%if params.has_key?(:family_id) %>
		<%= link_to 'Edit', edit_phone_number_path(@phone_number) %> |
		<%= link_to 'Back', phone_numbers_path %>
<%elsif params.has_key?(:person_id) %>
		<%= link_to 'Update', edit_family_address_path(@family,@address) %> |
		<%= link_to 'Back', person_path %>
<%end %>
