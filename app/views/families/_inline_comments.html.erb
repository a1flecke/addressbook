<% if family.comments.empty? %>
	<%= link_to "Add Comment", family_comments_path(family), :class => 'btn btn-mini btn-info' %>
<% else %>
	<%= link_to "Comments", family_comments_path(family) %>
<% end %>